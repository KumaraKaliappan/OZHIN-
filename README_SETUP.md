Local setup instructions:
1. Install Node.js and npm.
2. Start MongoDB or use MongoDB Atlas and set MONGODB_URI in .env.
3. From project root: cd backend && npm install
4. Copy .env.example to .env and fill values.
5. Start server: node server.js
6. Open http://localhost:4000 and admin at http://localhost:4000/admin/index.html
